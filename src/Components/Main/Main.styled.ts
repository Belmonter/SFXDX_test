import styled from "styled-components";
import {rem} from "../../StyledComponents/Theme";

export const MainStyled = styled.div`
  padding: ${rem(44)} ${rem(86)};
  width: 100%;
  height: 100%;
  flex: 1 1 auto;
`
