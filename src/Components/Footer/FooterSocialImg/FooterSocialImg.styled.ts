import styled from "styled-components";
import {rem} from "../../../StyledComponents/Theme";

const StyledFooterSocialImg = styled.img`
  width: ${rem(24)};
  height: ${rem(24)};
  object-fit: contain;
`

export default StyledFooterSocialImg
